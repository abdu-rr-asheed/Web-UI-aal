# Component pattern specifications

One file per component, in the `PRD.md` §9 table format. Generated by
`/pattern-spec <name>` and required by the Definition of Done (PRD §11.3).

Keyboard tables are extracted from the W3C APG by the `apg-spec-extractor`
subagent — never written from memory.
