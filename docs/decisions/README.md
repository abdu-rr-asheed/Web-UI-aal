# Architecture Decision Records

One decision per file, created with `/adr <title>`. The index lives in `PRD.md` Appendix D.

ADR-0001 … ADR-0011 are reserved by that index. Examiners read these — they are
where the *reasoning* behind the library lives, not just the outcome.
